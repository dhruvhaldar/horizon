1. Change the order of CORS and BaseHTTPMiddleware so CORS wraps the app correctly, which fixes the problem with CORS not being applied on early return in combined_security_middleware.
   - I see the rule "In FastAPI/Starlette, decorators like `@app.middleware("http")` wrap the entire middleware stack added via `app.add_middleware()`, meaning they execute first (outermost). If this custom middleware intercepts a request and returns early... it bypasses middlewares added via `add_middleware` like `CORSMiddleware`... To fix, use `app.add_middleware(BaseHTTPMiddleware, dispatch=...)` and ensure `CORSMiddleware` is added last so it acts as the true outermost wrapper."
   - Wait, `CORSMiddleware` is currently added AFTER `BaseHTTPMiddleware`, which means `CORSMiddleware` wraps the app that ALREADY has `BaseHTTPMiddleware` applied. Wait, actually Starlette's `add_middleware` wraps the *existing* app with the new middleware.
     If `app.add_middleware(A)` then `app.add_middleware(B)`, B is the outermost middleware.
     Wait, in the file:
     ```
     app.add_middleware(BaseHTTPMiddleware, dispatch=combined_security_middleware)
     app.add_middleware(
         CORSMiddleware, ...
     )
     ```
     So CORS is added last, which means it wraps `BaseHTTPMiddleware`. This is ALREADY done. Let's check the memory: "To fix, use `app.add_middleware(BaseHTTPMiddleware, dispatch=...)` and ensure `CORSMiddleware` is added last so it acts as the true outermost wrapper."

Let's find the missing performance improvement. Let's check `api/index.py`
